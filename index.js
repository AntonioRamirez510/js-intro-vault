// create 3 variables whose calculations add up to 10, 40, and 39
const first = 5 + 5;
const second = 80/2;
const third = (second - 1);
// Display the combination in an alert pop-up
const combination = first + ' - ' + second + ' - ' + third;
const stringForUser = "You have received this message because you have been chosen to open an important vault. Here is the secret combination:\n"
alert(stringForUser + ' ' + combination);
